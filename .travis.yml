language: php

services:
  - docker

cache:
  directories:
    - vendor
    - $HOME/.composer/cache
    - /var/lib/docker/overlay2

before_install:
  - docker-compose up -d

install:
  - composer install

script:
  - ./run-tests.sh
