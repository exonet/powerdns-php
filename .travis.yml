language: php

services:
  - docker

cache:
  directories:
    - vendor
    - $HOME/.composer/cache

before_install:
  - docker-compose up -d

install:
  - composer install

script:
  - ./run-tests.sh
